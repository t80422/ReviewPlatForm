@model WebApplication1.Models.SubsidyEdit

@{
    Layout = "~/Views/Shared/Layout.cshtml";

    int perm = Session["perm"] != null ? (int)Session["perm"] : -1;
    // 1. 無登入-1
    // 2. 管理者0、PM1、工讀生2
    // 3. 業者
}

<main class="container-wrap">
    <div class="container-main">
        <div class="form-wrap">
            @using (Html.BeginForm("Create", "Subsidy", FormMethod.Post, new { id = "myForm", enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                @Html.HiddenFor(x => x.id_id)

                <div class="form-main">
                    <div class="form-title">
                        <h3>新增補助申請</h3>
                    </div>

                    <div class="form-content">
                        <div class="form-flex">
                            <div class="form-group">
                                <div class="form-col">
                                    <div class="form-input">
                                        <label class="required">申請文件</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.Application, new { @type = "file", @required = "required", @accept = ".pdf" })

                                            <span></span>

                                            <button class="onfile" type="button">上傳檔案</button>

                                            <div class="red-text">
                                                <p>*說明:包含申請書、切結書和領據需在同一文件<br>*說明:請傳pdf或jpg圖檔，每次上傳限制為5M以內<p>
                                            </div>
                                        </div>

                                        <label class="demo-file">
                                            <a href="~/assets/sample/申請書(範本檔).pdf" download>範本檔</a>
                                        </label>
                                    </div>

                                    <div class="form-input">
                                        <label for="file1" class="required">全公司勞保明細</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.Labor, new { @type = "file", @required = "required", @accept = ".pdf" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                            <div class="red-text">
                                                <p>*說明:請傳pdf或jpg圖檔，每次上傳限制為5M以內<p>
                                            </div>
                                        </div>

                                        <label class="demo-file">
                                            <a href="~/assets/sample/全公司勞保明細(範本檔).pdf" download>範本檔</a>
                                        </label>
                                    </div>

                                    <div class="form-input">
                                        <label class="required">建立全公司勞保清冊</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.EmployeeList, new { @type = "file", @required = "required", @accept = ".xlsx" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                            <div class="red-text">
                                                <p>*說明:本欄位請依範例上傳EXCEL<p>
                                            </div>
                                        </div>

                                        <label class="demo-file">
                                            <a href="~/assets/sample/全公司勞保清冊(範本檔).xlsx" download>範本檔</a>
                                        </label>
                                    </div>

                                    <div class="form-input">
                                        <div class="red-text">
                                            <p>提醒:<p><br />
                                            <p>1.第一次申請時,請提供申請人員6個月以上投保證明</p><br />
                                            <p>2.第二次及以後申請時,請附申請月份的當月在保證明</p><br />
                                            <p>3.全公司勞保明細請至勞保局申請上月最後一天的勞保明細，並根據勞保明細之勞保序號為順序，登打建立全公司勞保清冊<p>
                                        </div>
                                    </div>

                                    <div class="form-input">
                                        <label class="required">申請補助人員清冊</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.ApplicantsList, new { @type = "file", @required = "required", @accept = ".xlsx" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                            <div class="red-text">
                                                <p>*說明:申請補助人員清冊請務必按照範本檔提供之格式登打，確定存成csv格式再上傳<p>
                                                <p>*說明:本欄位請依範例上傳CSV格式<p>
                                            </div>
                                        </div>

                                        <label class="demo-file">
                                            <a href="~/assets/sample/申請人員清冊(範本檔).xlsx" download>範本檔</a>
                                        </label>
                                    </div>
                                </div>

                                <div class="form-col">
                                    <div class="form-input">
                                        <label class="required">申請金額</label>
                                        <div class="input">
                                            @Html.TextBoxFor(x => x.Money, new { @type = "number", @required = "required" })
                                        </div>
                                    </div>

                                    <div class="form-input">
                                        <div class="red-text">
                                            <p>說明: 請依規定符合資格者每人每月補助5000元<p><br />
                                        </div>
                                    </div>

                                    <div class="form-input">
                                        <label class="required">申請人數</label>
                                        <div class="input">
                                            @Html.TextBoxFor(x => x.EmpCount, new { @type = "number", @required = "required", @style = "width:150px", @min = "1" })
                                            <span>可申請人數：@(ViewBag.Maxmember- ViewBag.MemberCount) </span>
                                        </div>

                                    </div>

                                    <div class="form-input">
                                        <div class="red-text">
                                            <p>說明:每個申請單位申請補助之總人數不得超過房間數八分之一;不足1人者,以1人計</p>
                                        </div>
                                    </div>
                                    <div class="form-input">
                                        <label for="file5">補件資料1</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.OtherFile, new { @type = "file", @accept = "" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                        </div>
                                    </div>

                                    <div class="form-input">
                                        <label for="file5">補件資料2</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.s_else_two, new { @type = "file", @accept = "" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                        </div>
                                    </div>

                                    <div class="form-input">
                                        <label for="file5">補件資料3</label>
                                        <div class="input file">
                                            @Html.TextBoxFor(x => x.s_else_three, new { @type = "file", @accept = "" })
                                            <span></span>
                                            <button class="onfile" type="button">上傳檔案</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="form-btn">
                    <button class="btn-cancel" type="button" onclick="history.back()">返回</button>
                    <button class="btn-submit" type="submit">下一步</button>
                </div>
            }
        </div>
    </div>
</main>

<script>
        $(function () {
            $('.checkbox-wrap input[type="checkbox"]').on('change', function () {
                $(this).siblings('.btn-correct').toggleClass('correct').toggleClass('notyet')
            })
        })
</script>

<script>
    $('#myForm').submit(function () {
        $('#myForm input').attr('required', false)
    })
</script>

<script>
    /*20231003 add by kevin*/
    $('#Application').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#Application').val('');
        }
    });
    $('#Labor').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#Labor').val('');
        }
    });
    $('#EmployeeList').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#EmployeeList').val('');
        }
    });
    $('#ApplicantsList').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#ApplicantsList').val('');
        }
    });
    $('#OtherFile').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#OtherFile').val('');
        }
    });
    $('#s_else_two').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#s_else_two').val('');
        }
    });
    $('#s_else_three').change(function () {
        if (this.files[0].size > 5000000) {
            alert("檔案大小超過5MB");
            $('#s_else_three').val('');
        }
    });
</script>

